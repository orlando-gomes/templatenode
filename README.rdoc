Com sequelize, bcryptjs já incluídos.
Para utilizar, apagar o yarn.lock e dar
um comando $ yarn;

Migrations:
$ yarn sequelize migration:create --name=create-users

Seeds:
$ yarn sequelize seed:generate --name= admin-user
$ yarn sequelize db:seed:all
